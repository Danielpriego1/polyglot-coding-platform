version: "3.8"

services:
  play:
    build: .
    container_name: polyglot-play
    ports:
      - "8000:8000"
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - DATABASE_URL=${DATABASE_URL:-}
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}

volumes:
  pgdata:
